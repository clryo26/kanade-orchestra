from __future__ import annotations

from dataclasses import dataclass


@dataclass
class ChatService:
    """Placeholder service for future chat assistant features."""

    def reply(self, message: str) -> str:
        return ""
