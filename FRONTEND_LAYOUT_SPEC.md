# 奏オケポータル 画面レイアウト仕様書

最終更新: 2026-06-30

## 1. 目的

この資料は、実際の画面表示イメージを共有するためのレイアウト仕様書です。
画面の機能仕様は FRONTEND_DESIGN.md を正本とし、本資料は配置と見え方を扱います。

## 2. 全体フレーム

### 2.1 PC表示の基本構造

[ヘッダー: メニュートグル + ロゴ + ポータル名]
[ローディングバー]
[本文コンテナ]
  [団員パネル] または [管理パネル] または [システム管理パネル] または [ログインパネル]
[右側サイドドロワー(開いた時のみ)]

### 2.2 スマホ表示の基本構造

[ヘッダー: メニュートグル + ロゴ + ポータル名]
[ローディングバー]
[本文コンテナ(1カラム)]
  [現在タブのカード群]
[全画面に近い右スライドドロワー(開いた時)]

## 3. ログイン画面レイアウト

### 3.1 ワイヤー

[中央寄せボックス]
  [タイトル: 略称ポータル]
  [名前入力]
  [パート選択]
  [パスワード入力]
  [ログインボタン]
  [更新ボタン][Rev表示]

### 3.2 配置ルール

- 1枚カードで縦積み
- 入力欄は全幅
- 操作ボタンは下部固定ではなく、フォーム末尾に配置
- ログイン前でも更新ボタンと Rev を表示

## 4. ポータルメインメニュー(member-home)レイアウト

### 4.1 ワイヤー(PC)

[お知らせカード]
  [最新5件のリスト]

[カウントダウンカード]
  [本番まであとN日]
  [演奏会名 / 日付]

[メニューカード]
  [グループ見出し]
  [ボタングリッド(固定幅カードを横並び)]
  [グループ見出し]
  [ボタングリッド]
  [下部アクション: マニュアル / ログアウト / 更新 / Rev]

### 4.2 ワイヤー(スマホ)

[お知らせカード]
[カウントダウンカード]
[メニューカード]
  [グループ見出し]
  [ボタングリッド(実質1列〜2列)]
  [下部アクション(横並び、狭い場合は折返し)]

### 4.3 メニューボタン仕様

- 通常ボタン: 白背景 + 境界線
- ホバー時: 薄いブランド色背景
- 管理者系ボタン: 警告色境界
- ボタン高さは統一し、ラベルは中央寄せ

### 4.4 演奏会情報グループの構成

- 演奏会情報
- 本番情報
- 楽曲紹介
- 楽譜ライブラリ
- 乗り番表

## 5. サイドドロワーレイアウト

### 5.1 ワイヤー

[ドロワーヘッダー]
  [メニュータイトル][閉じる]

[スクロール領域]
  [メニューグループ]
  [各グループは1列ボタン]

[下部アクション]
  [マニュアル][ログアウト][更新][Rev]

### 5.2 ふるまい

- 背面に半透明バックドロップ
- ドロワー外タップで閉じる
- メニュー選択後はドロワーを閉じて対象タブへ遷移

## 6. 管理パネルレイアウト

### 6.1 ワイヤー

[上部タブ列(折返し可)]
[選択タブのカード群]
  [入力フォームカード]
  [一覧カード]

### 6.2 レイアウト方針

- 編集系はカード単位で分割
- 一覧とフォームを同時表示し、作業往復を減らす
- モバイルではカードを縦1列に積む

### 6.3 本番情報管理のワイヤー

[演奏会選択]
[当日タイムテーブル(複数行)]
[本番衣装(男女別)]
  [男性: 上 / 下 / その他]
  [女性: 上 / 下 / その他]
[係り割テーブル(担当/氏名)][行追加]
[保存][Excel出力][選択解除][削除]
[登録済み本番情報リスト]

- タイムテーブル入力は複数行テキストを維持しつつ、保存時に `timeline_rows` へ自動分解して保持する
- 推奨入力形式:
  - `HH:MM 内容`
  - `HH:MM-HH:MM 内容`
  - `HH:MM 30分 内容`
- 曲名行の所要時間が未指定の場合、演奏会曲目の演奏時間（分）を使って補完する

## 7. 団員向け本番情報画面レイアウト

[演奏会名 + 日付]
[当日タイムテーブル]
[本番衣装(男女別テーブル)]
[係り割]

- 表示は演奏会単位のカードで縦積み
- 当日タイムテーブルは構造化済み `timeline_rows` がある場合は表形式（時間/内容/所要）で表示する
- 表示順は演奏会日付の新しい順で固定
- 各項目は未登録時に "未登録" と表示

## 7. システム管理パネルレイアウト

### 7.1 ワイヤー

[上部タブ列(認証端末/団体情報/SNS/接続先/パート/アクセスログ/DB)]
[選択タブの設定フォームまたは管理テーブル]

### 7.2 レイアウト方針

- 設定入力はフォーム主体
- 監査系(アクセスログ/DB状態)は表と結果表示を優先

## 8. レスポンシブ仕様(抜粋)

- メニューグリッドは PC で複数列、ドロワー内は1列
- ロゴサイズは画面幅に応じて縮小
- 主要コンテンツは常に縦スクロール可能
- タブ列は折返しで欠けを防止

## 9. 実装対応表

- ヘッダー/ドロワー骨組み: src/index.html
- メニュー生成ロジック: src/static/js/app.js
- ポータルホーム描画: src/static/js/app.js
- ログイン画面生成: src/static/js/auth_feature.js
- メニュー/ドロワー/ホーム系スタイル: src/static/css/style.css

## 10. 更新ルール

- 画面レイアウト変更時は本資料を先に更新する
- 機能仕様変更時は FRONTEND_DESIGN.md と合わせて更新する
- 変更実装後は UI のスクリーンショットを PR に添付する
