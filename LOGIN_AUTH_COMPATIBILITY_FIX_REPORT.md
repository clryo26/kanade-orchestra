# ログイン認証互換性修正レポート

## 原因

DB運用基盤追加後、ログイン時のパスワード検証が既存データの形式に十分対応できず、
正しいパスワードでも `Invalid member password` になる可能性がありました。

特に以下のケースでログインに失敗しやすい状態でした。

- 旧形式のSHA256ハッシュ
- Django / Werkzeug系のPBKDF2形式
- 管理画面表示用の `設定済み` 等が誤って password に保存されたデータ
- iPhone等の入力で前後に空白や不可視文字が入った場合
- ローカルテスト環境で google-cloud-storage が無く、バックエンド import が失敗する場合

## 修正内容

### 1. パスワード検証の互換性強化

`src/backend/services/security_service.py` を修正し、以下の形式に対応しました。

- 現行 `pbkdf2$sha256$...`
- 旧 `sha256$<salt>$<hash>`
- 64文字のSHA256 hex
- Django形式 `pbkdf2_sha256$...`
- Werkzeug形式 `pbkdf2:sha256:...`
- bcrypt形式
- 旧平文パスワード

### 2. 入力ゆれ対応

ログインパスワードについて、完全一致を優先しつつ、互換目的で以下の候補も検証します。

- 前後空白除去
- NFKC正規化
- 不可視文字除去

### 3. passwordプレースホルダー対応

`設定済み` / `********` など、画面表示用文字列が誤ってDBへ保存された場合は、
パスワード未設定扱いにしてパスワード登録画面へ誘導するよう修正しました。

### 4. GCS依存のimport安全化

ローカルテスト環境などで `google-cloud-storage` が無い場合でも、
アプリ全体のimportが失敗しないよう `drive_storage.py` を修正しました。

## 確認

- Python構文チェック実施済み
- `src.backend.main` のimport確認済み

## 注意

本修正はログイン互換性を広げるためのものです。
本番反映後、ログインできない団員がまだいる場合は、その団員の `members.password` に
不正な値が入っていないか確認してください。
